import React from 'react';
import ReactDOM from 'react-dom';

const AddLocation = React.createClass({
        render() {
    		 return (<div>This is where the location will be uploaded</div>);
    	}
      
});
 export default AddLocation;